# QQ音乐歌曲信息爬取与数据分析系统实验报告

## 一、实验背景与目标

&emsp;&emsp;本次实验围绕音乐网站中的歌曲与歌手展开，目标是完成一个从数据爬取、数据清洗、Web 展示到数据分析的完整项目。项目首先从 QQ 音乐中爬取歌曲、歌手、歌词、歌图片和评论等信息，并将结果保存为 JSON 文件；随后使用 Django 框架搭建音乐信息浏览网站，实现歌曲列表、歌曲详情、歌手列表、歌手详情、搜索和评论等功能；最后基于爬取到的数据进行统计分析，尝试从歌词重复率、歌词相似度和歌手出生年代等角度发现样本中的规律。

&emsp;&emsp;本实验不仅关注网页功能是否完整，也关注数据组织、搜索效率、页面跳转逻辑和数据分析结论的可靠性。因此，项目整体可以分为三个部分：第一部分是爬虫与数据整理，第二部分是 Web 系统设计，第三部分是音乐数据分析。

## 二、数据来源与爬虫设计

&emsp;&emsp;本项目的数据来源为 QQ 音乐网页端。爬虫主要围绕歌手和歌曲两个对象展开。对于歌手数据，程序首先获取歌手列表，再根据每位歌手的主页 URL 进入详情页，提取歌手姓名、歌手 ID、歌手简介、生日、国籍、代表作、荣誉记录、歌手图片 URL 等信息。对于歌曲数据，程序在歌手页面中继续获取其歌曲列表，并进入歌曲详情页提取歌曲 ID、歌曲名、歌手名、歌曲 URL、歌词、歌曲封面图片 URL 以及评论信息。

&emsp;&emsp;爬虫中使用 `requests` 获取普通页面和接口数据，使用 `BeautifulSoup` 解析 HTML 页面结构，使用正则表达式提取页面中隐藏的 ID、图片地址和歌词相关字段。由于 QQ 音乐部分页面需要渲染或滚动后才能完整显示，项目中还使用 `Playwright` 处理动态页面和评论区域，尽量保证爬取到的数据完整。

&emsp;&emsp;歌词数据通过 QQ 音乐歌词接口获取。接口返回的歌词内容包含时间轴和编码信息，程序在解析时会先进行解码，再去除时间戳，只保留真正的歌词文本。评论数据主要包含评论正文和评论时间，每首歌曲尽量采集 3 条评论，用于后续页面展示和数据分析。

&emsp;&emsp;在图片处理方面，爬虫不仅保存原始图片 URL，也会将图片下载到本地目录中，便于后续 Web 页面直接加载。歌手图片保存到 `static/images/singers/`，歌曲图片保存到 `static/images/songs/`。最终，歌手信息保存为 `singers.json`，歌曲信息保存为 `songs.json`，为 Django 网站和数据分析提供统一的数据来源。

## 三、数据结构与数据规模

本项目最终获得歌曲数据 2060 条，歌手数据 250 条，歌曲评论 6156 条。

歌曲数据的主要结构如下：

```text
song_id
song_url
song_title
song_singer
song_photo_url
song_photo_path
song_lyrics
song_comments
song_singer_id
song_singer_ids
song_singers
```

歌手数据的主要结构如下：

```text
singer_id
singer_name
singer_url
singer_photo_url
singer_photo_path
生日
国籍
职业
代表作
简介
从艺历程
荣誉记录
```

&emsp;&emsp;歌曲数据和歌手数据分别存储在 `music_site/data/songs.json` 和 `music_site/data/singers.json` 中。这样的设计使爬虫结果与 Web 系统之间保持相对独立：爬虫负责生成数据文件，Django 网站负责读取和展示数据，数据分析部分也可以直接复用同一份 JSON 数据。

## 四、Web 系统设计与功能实现

&emsp;&emsp;Web 系统使用 Django 框架实现，系统主页为歌曲列表页。歌曲列表页通过分页方式展示所有歌曲，每页展示 20 首歌曲，并提供首页、上一页、下一页、尾页、页码按钮和输入页码跳转功能。用户点击歌曲条目后可以进入歌曲详情页，查看歌曲封面、歌曲名、歌手、原始网站 URL、歌词和评论信息。

&emsp;&emsp;歌曲详情页中，歌手名可以点击跳转到对应的歌手详情页，歌曲原始 URL 可以跳转到 QQ 音乐原页面。页面下方展示爬取到的历史评论，同时实现了用户评论功能。用户可以提交新的评论，评论会保存到 SQLite 数据库中，并按照时间倒序显示。每条用户评论旁边提供删除按钮，可以删除对应评论。本项目没有设计用户登录系统，因此评论删除不区分用户权限。

&emsp;&emsp;歌手列表页同样采用分页展示，每页展示 20 位歌手。用户点击歌手条目后可以进入歌手详情页，查看歌手图片、简介、原始网站 URL 等信息。歌手详情页还会列出该歌手在系统中的所有歌曲，点击歌曲条目可以继续跳转到歌曲详情页。

&emsp;&emsp;页面模板主要位于 `music_site/music/templates/music/` 目录下，css文件位于 `music_site/music/static/music/` 目录下。系统通过统一的基础模板写入导航栏、搜索入口和页面主体，使不同页面之间能够通过页面控件来回跳转，不需要用户手动输入 URL。

## 五、搜索功能与性能优化

&emsp;&emsp;搜索功能支持歌曲搜索和歌手搜索两种类型。搜索入口包含关键词输入框、搜索类型选择和搜索按钮。用户输入 20 个字符以内的关键词后，系统根据选择的类型进行检索，并跳转到搜索结果页。

&emsp;&emsp;歌曲搜索考虑的字段包括歌曲名、歌手名和歌词内容；歌手搜索考虑的字段包括歌手名和歌手简介。当前实现采用子串匹配方式，即只要关键词出现在对应字段中，就认为该条记录符合条件。搜索结果页会显示结果数量、后端搜索耗时，并以分页形式展示搜索结果。每个结果条目包含名称和图片，点击后可以进入对应的歌曲详情页或歌手详情页。

&emsp;&emsp;为了提高搜索效率，系统在 Django 视图中对 JSON 数据进行了缓存。歌曲列表、歌手列表、歌曲 ID 映射和歌手 ID 映射在第一次读取后会保存在全局变量中，后续请求不需要重复读取 JSON 文件，能够满足实验要求中搜索时间不超过 1 秒的目标。

## 六、数据分析设计与结论

&emsp;&emsp;数据分析部分已经单独整理在 `data_analysis/音乐数据分析.ipynb` 中，实验过程中对应代码也整理到了 `data_analysis/analysis.py`。因此，本实验报告中只对分析思路和主要结论做简要说明。该部分使用 `json`、`re`、`jieba`、`numpy`、`matplotlib` 和 `scikit-learn` 等 Python 库，对歌曲歌词、评论和歌手信息进行统计分析，并通过图表辅助展示结果。

&emsp;&emsp;本次数据分析主要围绕三个问题展开。第一，分析歌词重复率的整体分布，并观察歌词重复率与评论平均长度之间是否存在关系。结果显示，2053 首有效歌曲的平均歌词重复率为 38.48%，其中 81.25% 的歌曲位于 20% 至 60% 区间；但歌词重复率与评论平均长度的相关系数约为 -0.016，二者几乎没有线性关系。

&emsp;&emsp;第二，比较同歌手歌曲和不同歌手歌曲之间的歌词相似度。分析中使用 `jieba` 分词，并通过 TF-IDF 与余弦相似度衡量歌词文本相似程度。结果显示，同歌手歌曲的平均相似度为 0.0876，不同歌手歌曲的平均相似度为 0.0238，前者约为后者的 3.68 倍，说明同一歌手的歌曲在用词和主题上可能具有一定延续性。

&emsp;&emsp;第三，分析歌手出生年代与歌词重复率之间的关系。程序从歌手生日字段中提取出生年份，并按照十年为单位分组统计。结果显示，在本次样本中，1950 年代和 1960 年代出生歌手的样本歌曲平均重复率均超过 44%；从 1960 年代以后，各年代平均重复率整体下降，到 2000 年代降至 32.71%。不过，由于缺少歌曲发行年份，且各年代样本量并不那么均衡，该结论只适用于本次样本观察，不能直接推广为整个音乐市场的发展规律。

## 七、项目技术与代码结构

本项目使用的主要技术如下：

```text
爬虫部分：requests、BeautifulSoup、Playwright、正则表达式
数据存储：JSON、SQLite
Web 框架：Django
前端展示：HTML、CSS
数据分析：NumPy、Matplotlib、jieba、scikit-learn
```

项目主要目录结构如下：

```text
crawler/
  crawler_clean.py
  requirements.txt

music_site/
  data/
    songs.json
    singers.json
  static/
    images/
      songs/
      singers/
  music/
    models.py
    views.py
    urls.py
    templates/
    static/

data_analysis/
  音乐数据分析.ipynb
  analysis.py
  output/
```

&emsp;&emsp;其中，`crawler_clean.py` 负责爬取、清洗和保存数据；`music_site/music/views.py` 负责读取数据、分页展示、详情页渲染、搜索和评论处理；`music_site/music/models.py` 中定义了评论模型；`data_analysis/音乐数据分析.ipynb` 负责完成数据分析和图表绘制。

## 八、实验总结与不足

&emsp;&emsp;本次实验过程中，我个人感觉收获颇丰。最开始写爬虫时，我主要只会根据 HTML 页面发送 `request` 请求，再用 `BeautifulSoup` 解析页面，并通过正则表达式定位需要的信息。后来在实际爬取 QQ 音乐数据时发现，图片链接、歌词、评论和歌手列表（需要滚动鼠标）等信息并不都直接存在于静态 HTML 中，很多内容需要动态加载。为了解决这些问题，我进一步学习了歌词接口的调用方式，也学习了使用 `Playwright` 控制浏览器，从而获取需要滚动或渲染后才能出现的数据。

&emsp;&emsp;在处理歌曲和歌手关系时，我也遇到了比较典型的数据结构问题。一开始我只根据页面上较明显的信息记录了一个歌手，后来在实现网页跳转时发现，有些歌曲并不止一个歌手。请教同学后，我才注意到 HTML 中实际上存放了所有歌手的链接，于是重新补全了歌曲对应的歌手 ID 列表。随后又发现，有些歌曲的所有歌手并不一定都存在于我已经爬取到的歌手数据中，因此我又设计了清洗函数，尽量保证保留下来的歌曲中，涉及到的歌手都能够在现有歌手数据中找到。仅爬虫和数据清洗部分前后加起来就至少花费了 30 个小时。

&emsp;&emsp;后来开始搭建 Web 系统，这一部分在理解 Django 的基本原理后相对清晰一些，但实际完成时仍然花费了不少时间。由于一开始 HTML 结构写得不够规范，后来在编写 CSS、并试图把 CSS 中设计好的样式模板应用到各个页面时，又遇到了新的困难。为了让页面结构更适合复用样式，我重新调整了部分 HTML 的层级和类名。虽然视图函数本身写起来相对直接，但分页、搜索、详情页跳转、评论提交和删除等功能串联起来后，整体也耗费了约 20 个小时。

&emsp;&emsp;最后的数据分析部分是我觉得比较有趣的一部分。在阅读相关文档时，我对 TF-IDF 向量化方法产生了兴趣，因为它可以把歌词文本转换为可计算的向量，并进一步比较文本之间的相似度。最后我成功基于 TF-IDF 和余弦相似度设计出了一个可用的探究方向，并得到了同歌手歌曲相似度更高这一较有意义的结论。除此之外，歌词重复率、评论长度和歌手出生年代等分析也让我更熟悉了使用 Python 对爬取数据进行统计、可视化和解释的流程。

&emsp;&emsp;当然，项目目前仍存在一些不足。首先，爬虫依赖 QQ 音乐当前页面结构，如果网站结构发生变化，部分选择器、接口或正则表达式可能需要调整。其次，搜索功能目前采用线性搜索方式，在当前数据规模下速度足够，但如果数据量进一步增大，可能就会面临效率偏低的问题。再次，数据分析中对歌词重复、相似度和出生年代的讨论仍然较初步，因此结论更适合作为本次样本中的观察，而不能直接推广到整个音乐市场。

&emsp;&emsp;总体来看，本实验基本实现了课程要求的爬虫、Web 系统和数据分析功能，也让我对一个完整 Python 项目的开发流程有了更具体的认识。后续如果继续完善，我想可以进一步增加数据规模、补充歌曲发行年份、优化搜索算法，并对评论文本进行情感分析或关键词分析，使系统具有更强的信息检索和数据分析能力。
